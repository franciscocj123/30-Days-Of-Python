
Level 1 Exercises
#1
#Get user input using input(“Enter your age: ”). If user is 18 or older, 
#give feedback: You are old enough to drive. If below 18 give feedback to 
#wait for the missing amount of years. Output

users_age = int(input("Enter your age: "))

if users_age >= 18:
    print("You are old enough to drive.")
if users_age < 18:
    years_left = 18 - users_age
    print(f"You are not old enough to drive. You need to wait {years_left} years")

#2
#Compare the values of my_age and your_age using if … else. Who is 
#older (me or you)? Use input(“Enter your age: ”) to get the age as 
#input. You can use a nested condition to print 'year' for 1 year 
#difference in age, 'years' for bigger differences, and a custom text if 
#my_age = your_age. Output

users_age = int(input("Enter your age: "))
my_age = 28

if users_age > my_age:
    age_gap = users_age - my_age
    print(f"You are older than me by {age_gap} years.")
elif my_age > users_age:
    age_gap = my_age - users_age
    print(f"I am older than you by {age_gap} years.")


#3
#Get two numbers from the user using input prompt. If a is greater than 
#b return a is greater than b, if a is less b return a is smaller than b, 
#else a is equal to b. Output

a = int((input("Enter number for a: ")))
b = int(input("Enter number for b: "))

if a > b:
    print(f"{a} is greater than {b}")
elif b > a:
    print(f"{b} is greater than {a}")
else:
    print(f"{a} and {b} are equal")


#Level 2 Exercices
#1
#Write a code which gives grade to students according to theirs scores:


students_grade = int(input("Enter grade percentage: "))

if students_grade >= 90:
    print("You have a A")
elif students_grade >= 80:
    print("You have a B")
elif students_grade >= 70:
    print("You have a C")
elif students_grade >= 60:
    print("You have a D")
elif students_grade <= 59:
    print("You have a D")


#2
#Get the month from user input then check if the season is Autumn, Winter, 
#Spring or Summer. If the user input is: September, October or November, the 
#season is Autumn. December, January or February, the season is Winter. March,
#April or May, the season is Spring June, July or August, the season is Summer


users_month = input("Enter month to check season: ").lower()

if users_month in ["september", "october", "november"]:
    print("The seaon is Autumn")
elif users_month in ["december", "january", "february"]:
    print("The season is Winter")
elif users_month in ["march", "april", "may"]:
    print("The season is Spring")
elif users_month in ["june", "july", 'august']:
    print("The season is Summer")


person={
    'first_name': 'Francisco',
    'last_name': 'Ceja',
    'age': 22,
    'country': 'United States',
    'is_married': True,
    'skills': ['JavaScript', 'React', 'Node', 'Css', 'Python', 'Html'],
    'address': {
        'street': 'Ball street',
        'zipcode': '2026'
    }
    }

#prints middle skills
middle_skill = len(person["skills"]) // 2
print(person["skills"][middle_skill])

#check if user has python skills
if "Python" in person["skills"]:
    print("You have Python!")
    

#If a person skills has only JavaScript and React, print('He is a front 
#end developer'), if the person skills has Node, Python, MongoDB, print('He 
#is a backend developer'), if the person skills has React, Node and MongoDB, 
#Print('He is a fullstack developer'), else print('unknown title') - for more
#accurate results more conditions can be nested!

if "JavaScript" in person["skills"] and "React" in person["skills"]:
    print("You are a front end developer.")
elif "Node" in person["skills"] and "Python" in person["skills"] and "MongoDB" in person["skills"]:
    print("You are a back end developer")
elif "React" in person["skills"] and "Node" in person["skills"] and "MongoDB" in person["skills"]:
    print("You are a full stack developer")


#If the person is married and if he lives in Finland, print the 
#information in the following format:
# Asabeneh Yetayeh lives in Finland. He is married.

if person["country"] == "United States" and person["is_married"] == True:
    print(f"{person["first_name"]} {person["last_name"]} lives in {person["country"]}. He is married")



























